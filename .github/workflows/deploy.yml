name: AWS Credentials from IAM Role

on:
  push:
    branches:
      - main

permissions:
  id-token: write # This is required for requesting the JWT
  contents: read # This is required for actions/checkou

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: prod
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Verify Secrets Existence
        run: |
          echo "AWS_ACCOUNT_ID is set: ${{ secrets.AWS_ACCOUNT_ID }}"
          echo "AWS_REGION is set: ${{ secrets.AWS_REGION }}"

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/github_actions
          aws-region: ${{ secrets.AWS_REGION }}
          retry-max-attempts: 5

      - name: Verify AWS Credentials
        run: aws sts get-caller-identity

      - name: Example AWS CLI command
        run: |
          aws s3 ls # Replace with your actual AWS commands
